---
status: template
---

# <Short title — one line, matches the file slug>

> **Template file.** Copy to `docs/ideas/<NNN>-<slug>.md` (NNN = highest existing ID + 1, zero-padded to 3 digits; slug in kebab-case, carrying the topic keyword for grep). Remove this banner and the YAML frontmatter when filling in real content. New file lives without any index entry — the directory listing IS the index. See the `workflow-idea` skill for the WHY behind the structure below.

**Status:** raw
**Origin:** YYYY-MM-DD · <one-line context — session, incident, conversation, whatever surfaced this>

## Core

<One paragraph. What the idea is at its essence. Write it the day the
idea is captured — do NOT rewrite this later. It is the seed, not the
sapling. Ripening happens in the Notes section below, not here.>

## Notes (chronological)

### YYYY-MM-DD
<First entry. What made this worth capturing? What's the immediate context?
Any adjacent thoughts, related things you noticed?>

<!--
Sections below are OPTIONAL. Add them when the idea grows into needing
them. Remove the header entirely if the section is empty — do NOT leave
placeholder text.

## Open questions
- <specific things that block promotion>

## Related
- [[NNN-other-idea]]
- REQ CUST-001
- ADR 0007
- T14

## Promotion
Filled only when Status becomes `ready` or `promoted`. One line:
`Promoted YYYY-MM-DD as [target-type]: [path or ID]`
-->
