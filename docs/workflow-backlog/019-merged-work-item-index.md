# Merged work-item index

## Landed — 2026-07-09

Consolidated `docs/tasks/index.md` into 5 columns with a `Type` column (`task | bug`). Flat `E<N>/` folders replaced the old `docs/tasks/epics/` intermediate directory. Sibling templates `_TEMPLATE_TASK.md` + `_TEMPLATE_BUG.md` live alongside the index at the top of `docs/tasks/`.
