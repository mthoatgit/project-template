---
status: template
---

# P<N> · <Sprechender Kurztitel — Verb + Subjekt>

> **Template file.** Kopiere zu `topics/<NNN>-<slug>.md` (NNN = höchste existierende ID + 1, dreistellig; slug in kebab-case und trägt das Topic-Keyword für Grep). Entferne dieses Banner und das YAML-Frontmatter beim Kopieren. Neuen Zeileneintrag in `index.md` im selben Commit hinzufügen — `Created` und `Updated` gleich (`YYYY-MM-DD HH:MM`).

- **Symptom.** <Was heute konkret bricht — Beispiele, IDs, konkrete Fundstellen.>
- **Impact.** <Warum es wichtig ist — welche Zukunftsarbeit leidet, oder was still verlorengeht.>
- **Proposed shape.** <Genug Skizze für Cold-Read. Kann sich beim Besprechen verfeinern.>
- **Source.** <Session-Datum, evtl. Zitat, evtl. `[[memory-file]]`-Link.>
