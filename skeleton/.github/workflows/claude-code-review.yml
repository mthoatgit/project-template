name: Claude Code Review

on:
  pull_request:
    types: [opened, synchronize, ready_for_review, reopened]

jobs:
  claude-review:
    uses: mthoatgit/workflows/.github/workflows/claude-review.yml@v1
    secrets: inherit
